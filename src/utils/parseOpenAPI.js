/**
 * Parses an OpenAPI spec into an array of StarData objects.
 * StarData type: { id, method, path, summary, description, tags, frequency, x, y, z, ... }
 */
export function parseOpenAPI(spec) {
  const stars = [];
  return stars;
}
